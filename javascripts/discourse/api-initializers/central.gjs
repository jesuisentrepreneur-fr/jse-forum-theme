/**
 * Generates dynamic CSS rules to display an emoji
 * before each category badge based on the `category_icons` setting.
 */
import { apiInitializer } from "discourse/lib/api";

export default apiInitializer("1.0", () => {});
